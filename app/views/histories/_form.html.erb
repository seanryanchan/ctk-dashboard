<%= form_with(model: history, local: true) do |form| %>
  <% if history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :date_action %>
    <%= form.datetime_select :date_action %>
  </div>

  <div class="field">
    <%= form.label :log_in_timestamp %>
    <%= form.datetime_select :log_in_timestamp %>
  </div>

  <div class="field">
    <%= form.label :log_out_timestamp %>
    <%= form.datetime_select :log_out_timestamp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
